import 'package:flutter/material.dart';

class SessionMate extends StatelessWidget {
  const SessionMate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
